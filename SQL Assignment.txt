Part 1Code for the ER diagram:# Modify this code to update the DB schema diagram.# To reset the sample schema, replace everything with# two dots ('..' - without quotes).Salary-emp_no PK FK >- emp.emp_nosalary intemployee as emp----emp_no PK inttitle int FK >- title.title_idfname stringlname stringbirth_date stringhire_date stringsex strinngdept as p # Table documentation comment 2------------dept_no PK intdept_name string  # Field documentation comment 3title----title_id PK inttitle UNIQUE stringdept_emp--emp_no string PK FK >- emp.emp_nodept_np string PK FK >- p.dept_nodept_man--dept_no string PK FK >- p.dept_noemp_no string PK FK >- emp.emp_noSQL Code:create table dept(dept_no varchar(15), dept_name varchar(50), primary key(dept_no));COPY dept(dept_no,dept_name)FROM '/09-SQL_09-SQL-Homework_Instructions_data_departments.csv'DELIMITER ','CSV HEADER;create table title(title_id varchar(15), title varchar(50), primary key(title_id));COPY title(title_id,title)                                                                                                                 FROM '09-SQL_09-SQL-Homework_Instructions_data_titles.csv'DELIMITER ','CSV HEADER;create table emp(emp_no varchar(15), title_id varchar(15), birth_date varchar(20), first_name varchar(50), last_name varchar(50), sex varchar(2),hire_date varchar(20),primary key(emp_no), foreign key(title_id) references title(title_id));COPY emp                                                                                                                                   FROM '09-SQL_09-SQL-Homework_Instructions_data_employees.csv'DELIMITER ','CSV HEADER;create table salary(emp_no varchar(15), salary integer, primary key(emp_no), foreign key (emp_no) references emp(emp_no));COPY salary                                                                                                                       FROM '09-SQL_09-SQL-Homework_Instructions_data_salaries.csv'DELIMITER ','CSV HEADER;create table dept_emp(emp_no varchar(15),dept_no varchar(15), primary key(emp_no,dept_no), foreign key (emp_no) references emp(emp_no), foreign key (dept_no) references dept(dept_no))COPY dept_emp                                                                                                                              FROM '/09-SQL_09-SQL-Homework_Instructions_data_dept_emp.csv'DELIMITER ','CSV HEADER;create table dept_man(dept_no varchar(15),emp_no varchar(15), primary key(dept_no,emp_no), foreign key (emp_no) references emp(emp_no), foreign key (dept_no) references dept(dept_no))COPY dept_manFROM '09-SQL_09-SQL-Homework_Instructions_data_dept_manager.csv'DELIMITER ','CSV HEADER;Part 21. select emp.emp_no, last_name, first_name, sex, salary from emp,salary where emp.emp_no = salary.emp_no;2. select first_name,last_name,hire_date from emp where RIGHT(hire_date,4) = '1986';3. select dept_man.dept_no, dept.dept_name, dept_man.emp_no, emp.last_name, emp.first_name from dept_man,dept,emp where dept.dept_no = dept_man.dept_no and dept_man.emp_no = emp.emp_no;4. select emp.emp_no, emp.last_name, emp.first_name, dept.dept_name from emp, dept, dept_emp where emp.emp_no = dept_emp.emp_no and dept_emp.dept_no = dept.dept_no;5. select first_name, last_name, sex from emp where first_name = 'Hercules' and LEFT(last_name,1) = 'B';6. select emp.emp_no, last_name, first_name, dept.dept_name from emp, dept, dept_emp where emp.emp_no = dept_emp.emp_no and dept_emp.dept_no = dept.dept_no and dept.dept_name = 'Sales';7. select emp.emp_no, last_name, first_name, dept.dept_name from emp, dept, dept_emp where emp.emp_no = dept_emp.emp_no and dept_emp.dept_no = dept.dept_no and (dept.dept_name = 'Sales' or dept.dept_name = 'Development');8. select last_name, count(last_name) from emp group by last_name order by count(last_name) desc;